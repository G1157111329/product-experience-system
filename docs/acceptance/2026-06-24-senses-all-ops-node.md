# 2026-06-24 五感体验完整操作节点验收

## 节点范围

- 进入服务器任务 `ef6811c9-cfb4-46c9-b83f-38ae3a80813e` 的 `五感体验` 模块。
- 覆盖证据栏与五感记录工作台的主要操作：拍照入口、录像入口、相册图片上传、新增非标准记录、素材绑定、完整编辑、素材选择器打开、素材删除、记录删除。
- 保护既有验收链路记录 `QP-SENSE-569915`，仅创建并清理临时测试数据。

## 浏览器交互与状态确认

- 打开 `五感体验` 后，页面可见既有记录 `QP-SENSE-569915`，状态为 `不合格`。
- 点击 `拍照` 后，相机采集弹窗可打开；点击 `录像` 后，录像采集弹窗可打开。
- 通过 `相册图片` 上传测试图片，POST `/api/materials/upload` 返回 `code=0`，生成素材 `2026062410370601.png` / `58837c94-54a4-4346-b31d-fd16e5f6cd2a`，页面证据栏可见该素材。
- 点击工作台 `新增`，选择 `非标准`，填写 `SENSE-FULL-615791`，标记 `不合格` 并提交；POST `/api/records` 返回 `code=0`，生成记录 `b34ab442-c7be-4f50-bd35-aa7beb086878`，页面列表可见新记录。
- 选中新记录后选择上传素材，点击 `绑定到当前五感记录`；PUT `/api/materials` 返回 `code=0`，读回 `/api/materials?task_id=...` 确认该素材 `record_id=b34ab442-c7be-4f50-bd35-aa7beb086878`。
- 点击 `完整编辑`，将临时记录改为 `SENSE-FULL-EDIT-615791`，状态改为 `合格` 并保存；PUT `/api/records/b34ab442-c7be-4f50-bd35-aa7beb086878` 返回 `code=0`，页面列表可见编辑后的标题和状态。
- 对既有记录 `QP-SENSE-569915` 打开 `完整编辑`，确认 `当前检查要求` 文案仍展示；点击 `选择素材` 后，素材选择器弹窗可打开并可读取素材列表。
- 在证据栏选中本轮上传素材和上一轮中断残留素材 `b0508532-3647-45f4-bac0-f144bd5c1309`，点击 `删除素材` 并确认；DELETE `/api/materials` 返回成功，读回素材列表确认两条测试素材均已删除。
- 选中临时记录 `SENSE-FULL-EDIT-615791`，点击记录操作区删除按钮；DELETE `/api/records/b34ab442-c7be-4f50-bd35-aa7beb086878` 返回 `code=0`，读回任务详情确认临时记录已删除。

## 补充拖拽与前端布局检查

- 补验素材拖拽绑定：上传临时素材 `2026062410452101.png` / `1e64578c-bec0-4a5d-a08b-0dae844568c0` 后，将素材从证据栏拖拽到 `QP-SENSE-569915` 记录卡；PUT `/api/materials` 返回成功，读回素材 `record_id=408140d8-c80e-46c5-94d0-0af01ffa5338`，确认拖拽绑定真实生效。
- 拖拽测试后通过证据栏删除该临时素材，读回任务素材数恢复为 `0`。
- 视觉检查发现桌面窄栏中 `相册图片 / 相册视频` 上传按钮被 `sm:grid-cols-4` 压缩，图标与文字有挤压风险。
- 修复方式：将证据栏上传入口固定为两列布局，保留原上传、拍照、录像、相册视频行为。
- 修复后服务器桌面截图确认四个上传按钮两列展示，文字和图标不再挤压；移动端滚动后确认五感记录列表与详情卡可正常查看，未被底部导航遮挡。

## 服务器数据库最终状态

- `check_records` 中该任务记录数为 `1`。
- 保留记录: `408140d8-c80e-46c5-94d0-0af01ffa5338 | QP-SENSE-569915 | 不合格 | 非标准 | Mock AI suggestion for quick preset sensory 569915`。
- `materials` 中该任务素材数为 `0`。
- `check_records` 中 `check_item like 'SENSE-FULL%'` 的临时记录数为 `0`。

## 结论

- 五感体验完整操作节点通过。
- 素材拖拽绑定可用。
- 本节点发现并修复 1 个前端布局缺陷：素材证据栏上传按钮在桌面窄栏挤压。
