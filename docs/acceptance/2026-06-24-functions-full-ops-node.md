# 2026-06-24 功能效果完整操作节点验收

## 节点范围

- 进入服务器任务 `ef6811c9-cfb4-46c9-b83f-38ae3a80813e` 的 `功能效果` 模块。
- 覆盖功能/食谱、步骤、素材证据、效果评价、问题点和 AI 按钮的主要操作。
- 保护既有验收链路功能 `QP-RECIPE-569915`，仅创建并清理临时测试数据。

## 浏览器交互与状态确认

- 打开 `功能效果` 后，页面可见既有功能 `QP-RECIPE-569915` 和 2 个步骤。
- 通过证据栏上传两张临时图片，POST `/api/materials/upload` 返回 `code=0`。
- 点击 `新增` 创建临时功能 `FUNC-FULL-729920`，选择类型 `功能`；POST `/api/recipes` 返回 `code=0`，生成 `f9fae066-5b1f-4f90-9764-2273e00ec36e`。
- 点击 `编辑` 将临时功能改为 `FUNC-FULL-EDIT-729920`；PUT `/api/recipes/[id]` 返回 `code=0`，页面和 API 均可读回新名称。
- 点击 `新增步骤` 新增两个步骤；POST `/api/recipe-steps` 均返回 `code=0`。
- 编辑第一条步骤为 `FUNC-STEP-1-EDIT-729920`，并填写步骤问题点；PUT `/api/recipe-steps/[id]` 返回 `code=0`。
- 点击步骤卡设置绑定目标，在证据栏选择素材并点击绑定；读回 `/api/materials?task_id=...` 确认素材 `recipe_step_id=f5f7fc2c-a00e-455a-8b16-97feafe6a184`。
- 将素材从证据栏拖拽到效果/出品评价卡；PUT `/api/materials` 返回 `code=0`，读回确认素材 `recipe_id=f9fae066-5b1f-4f90-9764-2273e00ec36e`。
- 打开效果素材 `选择素材` 弹窗，确认素材选择器可用。
- 填写效果评价描述和效果问题点，点击 `保存评价`；PUT `/api/recipes/[id]` 返回 `code=0`，读回 `/api/recipes?task_id=...` 确认 `effect_description` 和 `effect_problem_point` 已持久化，且拖拽绑定到效果评价的素材保存后未被解绑。
- 点击 `AI总结评分` 和 `AI识别问题点`，均触发对应 POST 请求；服务器返回 `AI服务连接失败: fetch failed`，按当前验收规则归为外部 AI 网络失败，不作为本节点产品缺陷。
- 删除临时步骤、临时素材和临时功能均通过 UI 完成，DELETE 接口返回成功。

## 拖拽排序补验

- 创建临时功能 `FUNC-ORDER-837900` 和两个步骤。
- 拖拽功能卡执行食谱/功能排序，PUT `/api/recipes` 返回 `code=0`。
- 拖拽第二个步骤到第一个步骤前，PUT `/api/recipe-steps` 返回 `code=0`；读回顺序为 `1:ORDER-STEP-B-837900`、`2:ORDER-STEP-A-837900`。
- 修复拖拽手柄触控区后再次补验步骤拖拽排序，读回顺序为 `POST-B-41017`、`POST-A-41017`。

## 前端布局与交互检查

- 桌面和移动端截图检查核心文案：`素材证据`、`功能/食谱`、`步骤时间线`、`效果/出品评价`、`AI总结评分`、`AI识别问题点` 均正常显示，无乱码。
- 发现并修复 1 个移动端交互缺陷：功能卡和步骤卡的编辑/删除按钮依赖 hover 才显示，移动端不可发现；拖拽手柄触控区仅 16x16。
- 修复后：移动端编辑/删除按钮常显，拖拽手柄和图标按钮触控区提升到 32x32；自动检查当前视口内无小于 24px 的可见控件。

## 服务器数据库最终状态

- `recipes` 中该任务功能数为 `1`。
- 保留功能: `f3fc3afc-9546-4c3e-a400-b40718092962 | QP-RECIPE-569915 | Function`。
- 保留功能步骤数为 `2`。
- `materials` 中该任务素材数为 `0`。
- 临时功能数为 `0`。

## 结论

- 功能效果完整操作节点通过。
- 本节点发现并修复 1 个前端交互缺陷：移动端功能/步骤编辑删除入口不可发现、拖拽手柄触控区过小。
